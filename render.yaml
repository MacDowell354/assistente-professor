services:
  - type: web
    name: assistente-professor-ia
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
#remover whisper e geração automática de índice
